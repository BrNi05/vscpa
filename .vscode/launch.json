{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug C++ Program (MAIN BUILDER)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/vscpa",  // Path to the compiled executable
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",  // Working directory
            "environment": [],
            "externalConsole": true,
            "MIMode": "gdb",  // Specify GDB for debugging
            "setupCommands": [
                {
                    "description": "Enable pretty-printing for gdb",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                }
            ],
            "logging": {
                "engineLogging": true
            },
            "preLaunchTask": "MAIN BUILDER",  // Task to build the code
            "miDebuggerPath": "C:\\msys64\\ucrt64\\bin\\gdb.exe"  // Update if you are on a Windows machine, and use a different gdb path
        }
    ]
}