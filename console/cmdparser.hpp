#ifndef CMDPARSER_HPP
#define CMDPARSER_HPP

namespace Parser
{
    // Processes CLI arguments and executes the corresponding actions
    void processCmd(int argc, char* argv[]);
}

#endif // CMDPARSER_HPP